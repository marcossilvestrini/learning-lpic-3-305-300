Vagrant.configure("2") do |config|
  config.vm.provider "docker" do |d|
    d.name = "vagrant-docker"
    d.image = "nginx"
    d.ports = ["8081:80"]
    d.volumes = ["/home/ricardo/Cursos/LPIC-3/vagrant/virtualbox/www-docs/:/usr/share/nginx/html"]
  end
end